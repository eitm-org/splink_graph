============
Installation
============

At the command line::

    $ easy_install splink_graph

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv splink_graph
    $ pip install splink_graph
